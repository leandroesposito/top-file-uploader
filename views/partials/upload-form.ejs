<div class="hidden-form-container">
  <button class="button reveal-form">
    + Upload File
  </button>
  <form action="/upload/<%= folder.id %>" method="POST" enctype="multipart/form-data" class="file-form hidden-form hidden">
    <div class="form-row">
      <label for="file">File</label>
      <input type="file" name="file" id="file" required>
    </div>
    <button class="button submit" type="submit">Submit</button>
  </form>
</div>
<script>
  const fileForm = document.querySelector(".file-form");
  const MAX_FILE_MB = 5
  const MAX_FILE_SIZE = 1024 * 1024 * MAX_FILE_MB;
  fileForm.addEventListener("submit", (event) => {
    const fileInput = fileForm.querySelector("#file");
    const file = fileInput.files[0];
    const fileSize = file.size;
    const fileMb = Math.floor(fileSize / 1024 / 1024 * 100) / 100;
    if (MAX_FILE_SIZE < fileSize){
      event.preventDefault();
      alert(`Your file size of ${fileMb}MB exceeds our limit of ${MAX_FILE_MB}MB.`);
    }
  })
</script>